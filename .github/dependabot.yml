# Dependabot — mises à jour des deps non-sécurité (les alertes CVE sont gérées
# séparément via Dependabot Security Updates côté repo settings).
# Config minimaliste : monthly + groupes par écosystème pour éviter le bruit.
version: 2
updates:
  # Crate Rust : uniffi, rusqlite, serde, uuid, etc.
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Paris"
    open-pull-requests-limit: 5
    target-branch: "dev"
    groups:
      cargo-all:
        patterns:
          - "*"
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
      - "rust"

  # Actions GitHub : checkout, rust-toolchain, etc.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Paris"
    open-pull-requests-limit: 3
    target-branch: "dev"
    groups:
      gha-all:
        patterns:
          - "*"
    commit-message:
      prefix: "chore(ci)"
    labels:
      - "dependencies"
      - "github-actions"

  # Gems Ruby : fastlane et ses dépendances transitives (Gemfile.lock racine).
  # Sans cette entrée l'écosystème n'était pas suivi du tout, alors que le repo
  # a bien un Gemfile.lock — d'où deux alertes restées ouvertes (faraday high,
  # excon medium) sans qu'aucune PR ne soit jamais proposée.
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Paris"
    open-pull-requests-limit: 3
    target-branch: "dev"
    groups:
      bundler-all:
        patterns:
          - "*"
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
      - "ruby"
